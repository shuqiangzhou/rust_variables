#[allow(unused)]
fn main() {
    //boolean类型只能使用于条件判断中
    let is_i32 = true;

    let is_u32 = false;

    let is_u32: bool = false;

    //对于结构体中的true和false
    let name = Name {};

}


struct Name{}